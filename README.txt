To compile just use the good ol' "make InsertsionSort"

As demonstrated in the Example.txt, the input accepted is any text document that has a number on each line. the program accepts up to 64 numbers, and expects that many. It should still work if you enter in less than 64 numbers, but will also display random numbers taken from the memory.